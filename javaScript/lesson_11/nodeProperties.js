/* các thuộc tính element
    clientHeight và clientWidth là chiều rộng và dài của element
    .atribute trả lại đối tượng giống array hiển thị các thuộc tính của element
    .autocaptitalize nếu là true là viết chữ trong element tự động viết hoa
    .autofocus bật con trỏ chuột nháy (chỉ hoạt động khi có thuộc tính conteneditable)
    .baseURI lấy ra địa chỉ hiện tại của web
    .childElementCount: đếm element con của element
    .childNode bao gồm cả textNode con và cả elemtn node
    .children trả về HTML collection về element node và node
    .classList quản lý class ,thêm , chỉnh, sửa
    .dragable true hoặc false thực hiện nghiệp vụ kéo thả
    .firstChild lấy ra node đầu tiên của element
    .firstElementchild lấy ra elementNode đầu tiên
    .nextElementSibling trả về thẻ element tiếp theo của element hiện tại
    .nextSibling trả về Node tiếp theo
    .nodeType nodeElement = 1, nodeAtribute = 2, nodeText = 3
    .spellcheck kiểm tra chính tả
*/

// thuộc tính contenteditable ="" thì có thể chỉnh sửa nội dung text trong thẻ đó và thẻ con của nó