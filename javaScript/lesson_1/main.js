/**
 * Hàm built-in
 * alert
 * Console in ra các phương thức bên tab console
 * Confirm giống alert những thêm cancel để xác nhận
 * Prompt giống Confirm nhưng thêm dòng input để ghi vô
 * Set timeout cho chạy đoạn code sau một khoảng thời gian được ấn định
 * Set interval giống set timeout nhưng sẽ chạy liên tục cứ mỗi thời gian đc ấn định
 */
